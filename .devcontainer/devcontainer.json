{
  "name": "Python Learn 2026",
  "image": "mcr.microsoft.com/devcontainers/python:1-3.13",
  "customizations": {
    "vscode": {
      "extensions": [
        "ms-python.python",
        "ms-python.vscode-pylance"
      ]
    }
  },
  // Проверяем наличие файла (-f) перед запуском, чтобы не прерывать сборку
  "updateContentCommand": "if [ -f .devcontainer/setup-folders.sh ]; then bash .devcontainer/setup-folders.sh; else echo 'Warning: setup script not found'; fi",

  // Установка зависимостей с проверкой наличия файла
  "postCreateCommand": "python3 -m pip install --upgrade pip && if [ -f requirements.txt ]; then pip install -r requirements.txt; fi",

  "forwardPorts": [5000]
}